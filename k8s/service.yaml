apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: default

spec:
  type: NodePort

  selector:
    app: project-fastapi

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30081
